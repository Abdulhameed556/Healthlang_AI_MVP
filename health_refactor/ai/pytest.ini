[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests that require external services (run separately)
addopts =
    --import-mode=importlib
    -v
    --cov=src
    --cov-report=term-missing
    --cov-fail-under=90
